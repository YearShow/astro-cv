<nav class="navbar">
    <span>Александр Ершов</span>
    <ul>
        <li><img src="/email.svg" alt="mail" /></li>
    </ul>
</nav>

<style>
    .navbar {
        z-index: 10;
        position: fixed;
        top: 0;
        left: 0;
        display: flex;
        justify-content: space-around;
        align-items: center;
        width: calc(100% - 40px);
        height: 50px;
        border-bottom: 1px solid rgb(30, 30, 30, .3);
        //background-color: rgb(255, 255, 255, .8);
        backdrop-filter: blur(8px) saturate(100%);
        padding: 10px 20px;
    }

    .navbar > span {
        font-weight: 600;
    }

    .navbar > ul {
        display: flex;
        align-items: center;
        list-style: none;
        gap: 30px;
        font-size: 14px;
    }

    .navbar > ul > li> img {
        width: 25px;
    }

    .navbar > ul > li > a {
        color: #1e1e1e;
        text-decoration: none;
        transition: all .3s;
    }
    .navbar > ul > li > a:hover {
        color: orange;
    }

    .navbar > ul > .contact-btn {
        background-color: #1e1e1e;
        padding: 5px 10px;
        border-radius: 5px;
    }

    .navbar > ul > .contact-btn > a {
        color: #fff;
    }
</style>